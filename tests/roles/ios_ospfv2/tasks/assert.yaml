- name: "assert show_ip_ospf_process_rib"
  assert:
    that:
      -  network.wc in show_ip_ospf_process_rib.stdout
      -  network.net in show_ip_ospf_process_rib.stdout
  loop: "{{ networks }}"
  loop_control:
    loop_var: network
  when:
    - networks is defined
    - networks is iterable
